% Problem: tests.theorems.barbers
formulas(assumptions).
    all shaver customer (((Barber(shaver) & Person(customer) & - ( Shaves(customer, customer) )) -> Shaves(shaver, customer))).
    all name ((Barber(name) -> Person(name))).
end_of_list.

formulas(goals).
end_of_list.