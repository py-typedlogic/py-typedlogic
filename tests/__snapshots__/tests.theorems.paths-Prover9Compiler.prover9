% Problem: tests.theorems.paths
formulas(assumptions).
    all x y z (((Path(x, y) & Path(y, z)) -> Path(x, z))).
    all source target ((Link(source, target) -> Path(source, target))).
end_of_list.

formulas(goals).
end_of_list.