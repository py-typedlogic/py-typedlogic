% Problem: tests.theorems.import_test.ext
formulas(assumptions).
    all name age ((Person(name, age) -> NamedThing(name))).
    all subject predicate object reciprocated ((Likes(subject, predicate, object, reciprocated) -> Relationship(subject, predicate, object))).
end_of_list.

formulas(goals).
end_of_list.